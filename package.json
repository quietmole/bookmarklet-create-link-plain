{
  "name": "create-link-plain",
  "description": "My spectacular module",
  "version": "0.0.0",
  "author": {
    "name": "sanemat",
    "email": "o.gata.ken@gmail.com",
    "url": "sane.jp"
  },
  "dependencies": {},
  "devDependencies": {
    "browserify": "^11.2.0",
    "conventional-changelog": "^0.5.0",
    "eclint": "^1.1.5",
    "eslint": "^1.7.1",
    "espower-loader": "^1.0.0",
    "espowerify": "^1.0.0",
    "fixpack": "^2.2.0",
    "globstar": "^1.0.0",
    "lodash.startswith": "^3.0.1",
    "mocha": "^2.3.3",
    "npm-check-updates": "^2.3.2",
    "power-assert": "^1.1.0",
    "rimraf": "^2.4.3",
    "testem": "^0.9.8",
    "touch": "^1.0.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "files": [
    "index.js"
  ],
  "keywords": [
    ""
  ],
  "license": "MIT",
  "repository": "sanemat/create-link-plain",
  "scripts": {
    "build:browser": "exit 1",
    "build:example-bundled": "exit 1",
    "build:test-bundled": "browserify -t espowerify test.js > test-bundled.js",
    "changelog": "touch changelog.md && conventional-changelog -i changelog.md --overwrite --preset angular",
    "clear:test-bundled": "rimraf ./test-bundled.js",
    "eclint": "globstar --ignore \"{node_modules/**,test-bundled.js}\" -- eclint check \"*.{js,yml,md,json}\"",
    "eslint": "eslint --ignore-path .gitignore index.js test.js",
    "fixpack": "fixpack",
    "git:tag": "git tag v${npm_package_version}",
    "lint": "npm run eclint && npm run eslint",
    "ncu": "ncu -u",
    "test": "testem",
    "verify": "npm run lint && npm run test"
  }
}
